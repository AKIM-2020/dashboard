import React from "react";
import {EditableTable} from "../../components";

const Cashier = ({data}) => {
  return
<
  div >
  < EditableTable
  tableData = {data}
  />
  < /div>
}

export default Cashier;
